{
  "name": "trimly",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=20.11.0", "pnpm": ">=9.0.0" },
  "scripts": {
    "dev": "corepack pnpm --parallel --filter ./frontend --filter ./backend dev",
    "dev:frontend": "corepack pnpm --dir frontend dev",
    "dev:backend": "corepack pnpm --dir backend start:dev",
    "build": "corepack pnpm --filter ./frontend build && corepack pnpm --filter ./backend build"
  }
}
